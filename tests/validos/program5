module miElemento as Canvas {
    Template: {
        top:${miVariable}
    }
    Script:{
        let count = 0, miVariable
        if(count < 0)
            miVariable = "negativo"
        else
            miVariable = "positivo"
    }
}
